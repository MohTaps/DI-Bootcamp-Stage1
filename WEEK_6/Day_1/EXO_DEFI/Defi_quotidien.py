# question 1
select count(*) from actors;

# question 2

insert into actors (first_name, last_name, age, number_oscars) values('','','', );

# Ceci nous genere un code d'erreur car il existe la contrainte NOT NULL